<UserControl x:Class="HotelManagementSystem.Views.MyReservationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HotelManagementSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Rezervările Mele" FontSize="24" FontWeight="Bold" Margin="20" Foreground="#2C3E50"/>

        <!-- EMPTY STATE: Visible only when HasReservations is False -->
        <Grid Grid.Row="1" Visibility="{Binding EmptyStateVisibility}">
            <!-- Note: Using dedicated ViewModel property for visibility logic -->
             <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Placeholder Image or Icon could go here -->
                <TextBlock Text="Nu ați făcut nicio rezervare." FontSize="18" Foreground="#7F8C8D" HorizontalAlignment="Center" TextAlignment="Center"/>
                <TextBlock Text="Rezervați acum un sejur de vis!" FontSize="14" Foreground="#95A5A6" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>

        <!-- LIST: Visible only when HasReservations is True -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Visibility="{Binding UserHasReservationsVisibility}">
            <ItemsControl ItemsSource="{Binding ReservationsList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#ECF0F1" CornerRadius="8" Margin="20,5,20,5" Padding="15">
                             <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="Rezervat pe: " Foreground="#95A5A6" FontSize="12"/>
                                        <TextBlock Text="{Binding CreationDate, StringFormat=d}" Foreground="#95A5A6" FontSize="12"/>
                                    </StackPanel>

                                    <TextBlock Text="{Binding Status}" FontWeight="Bold" Foreground="#34495E" Margin="0,0,0,5"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="Check-in: " Foreground="#7F8C8D"/>
                                        <TextBlock Text="{Binding CheckInDate, StringFormat=d}" FontWeight="SemiBold"/>
                                        <TextBlock Text=" - Check-out: " Foreground="#7F8C8D" Margin="10,0,0,0"/>
                                        <TextBlock Text="{Binding CheckOutDate, StringFormat=d}" FontWeight="SemiBold"/>
                                    </StackPanel>

                                    <!-- Room Details List -->
                                    <ItemsControl ItemsSource="{Binding Rooms}" Margin="0,5">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <TextBlock Text="Camera: " Foreground="#7F8C8D"/>
                                                    <TextBlock Text="{Binding RoomNumber}" FontWeight="Bold" Foreground="#2C3E50"/>
                                                    <TextBlock Text=" (" Foreground="#7F8C8D"/>
                                                    <TextBlock Text="{Binding Type}" Foreground="#34495E"/>
                                                    <TextBlock Text=")" Foreground="#7F8C8D"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <TextBlock Text="{Binding TotalPrice, StringFormat='Total: {0} RON'}" FontSize="14" Foreground="#27AE60" FontWeight="Bold" Margin="0,5,0,0"/>
                                </StackPanel>

                                <!-- Action Button (e.g. Cancel or View Details) could go here -->
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
