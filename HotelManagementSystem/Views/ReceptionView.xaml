<UserControl x:Class="HotelManagementSystem.Views.ReceptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Background="#ECF0F1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <!-- Sidebar -->
            <ColumnDefinition Width="*"/>
            <!-- Continut -->
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <StackPanel Grid.Column="0" Background="#2C3E50">
            <TextBlock Text="RECEPȚIE" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,30" HorizontalAlignment="Center"/>

            <Button Content="Hartă Camere / Rezervări" Command="{Binding ShowMapCommand}" Style="{StaticResource SidebarButtonStyle}"/>
            <Button Content="Cereri Online (Confirmări)" Command="{Binding ShowRequestsCommand}" Style="{StaticResource SidebarButtonStyle}"/>

            <Separator Background="#34495E" Margin="10,20"/>
            <!-- Buton logout -->
            <Button Content="Logout" Command="{Binding LogoutCommand}" Background="#E74C3C" Foreground="White" Margin="10"/>
        </StackPanel>

        <!-- Zona unde se afișează fie Harta, fie Lista -->
        <Border Grid.Column="1" Margin="10" Background="White" CornerRadius="5">
            <ContentControl Content="{Binding CurrentReceptionView}"/>
        </Border>
    </Grid>
</UserControl>