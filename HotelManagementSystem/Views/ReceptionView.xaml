<UserControl x:Class="HotelManagementSystem.Views.ReceptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HotelManagementSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Margin="20">
        <StackPanel>
            <TextBlock Text="Cereri Rezervări în Așteptare" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

            <DataGrid ItemsSource="{Binding PendingReservations}" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Client" Binding="{Binding User.FullName}" Width="*"/>
                    <DataGridTextColumn Header="Data Sosire" Binding="{Binding CheckInDate, StringFormat=d}" Width="100"/>
                    <DataGridTextColumn Header="Data Plecare" Binding="{Binding CheckOutDate, StringFormat=d}" Width="100"/>
                    <DataGridTextColumn Header="Total Pers." Binding="{Binding NrPersons}" Width="70"/>
                    <DataGridTemplateColumn Header="Acțiuni" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Confirmă" Command="{Binding DataContext.ConfirmCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                    CommandParameter="{Binding}" Background="#27AE60" Foreground="White"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
