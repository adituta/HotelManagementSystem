<UserControl x:Class="HotelManagementSystem.Views.ReceptionConfirmationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
            <!-- HEADER -->
            <Border Grid.Row="0" BorderBrush="#DDD" BorderThickness="0,0,0,1" Margin="0,0,0,20" Padding="0,0,0,10">
                <DockPanel>
                    <TextBlock Text="Cereri Rezervări în Așteptare" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding PendingReservations.Count}" FontWeight="Bold" Foreground="#E74C3C" FontSize="24" Margin="0,0,5,0"/>
                        <TextBlock Text="cereri noi" VerticalAlignment="Bottom" Foreground="#7F8C8D" Margin="0,0,0,4"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- LISTA CARTOANE -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding PendingReservations}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="280" Background="White" CornerRadius="8" Margin="0,0,20,20" Padding="0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.1" Color="#000"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/> <!-- Header Color -->
                                        <RowDefinition Height="Auto"/> <!-- Content -->
                                        <RowDefinition Height="Auto"/> <!-- Footer Actions -->
                                    </Grid.RowDefinitions>

                                    <!-- Banner Culoare -->
                                    <Border Grid.Row="0" Background="#3498DB" CornerRadius="8,8,0,0" Height="6"/>

                                    <!-- Continut -->
                                    <StackPanel Grid.Row="1" Margin="15">
                                        <!-- User Avatar / Name -->
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                            <Border Width="40" Height="40" CornerRadius="20" Background="#ECF0F1" Margin="0,0,10,0">
                                                <TextBlock Text="{Binding User.FullName, StringFormat='{}{0:1}'}" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                           FontWeight="Bold" Foreground="#7F8C8D"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding User.FullName}" FontWeight="Bold" FontSize="16" Foreground="#2C3E50"/>
                                                <TextBlock Text="{Binding User.PhoneNumber}" FontSize="12" Foreground="#95A5A6"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <Separator Background="#F0F2F5" Margin="0,0,0,10"/>

                                        <!-- Detalii Sejur -->
                                        <Grid Margin="0,0,0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="SOSIRE" FontSize="10" Foreground="#BDC3C7" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding CheckInDate, StringFormat='dd MMM'}" FontWeight="Bold" FontSize="14" Foreground="#2C3E50"/>
                                                <TextBlock Text="{Binding CheckInDate, StringFormat='yyyy'}" FontSize="11" Foreground="#95A5A6"/>
                                            </StackPanel>
                                            
                                            <Path Grid.Column="1" Data="M0,0 L10,5 L0,10" Stroke="#BDC3C7" StrokeThickness="2" VerticalAlignment="Center" Margin="10,0"/>

                                            <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                                <TextBlock Text="PLECARE" FontSize="10" Foreground="#BDC3C7" FontWeight="Bold" HorizontalAlignment="Right"/>
                                                <TextBlock Text="{Binding CheckOutDate, StringFormat='dd MMM'}" FontWeight="Bold" FontSize="14" Foreground="#2C3E50" HorizontalAlignment="Right"/>
                                                <TextBlock Text="{Binding CheckOutDate, StringFormat='yyyy'}" FontSize="11" Foreground="#95A5A6" HorizontalAlignment="Right"/>
                                            </StackPanel>
                                        </Grid>

                                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                            <Border Background="#F0F2F5" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                                                <TextBlock Text="{Binding NrPersons, StringFormat='{}{0} Persoane'}" FontSize="12"/> 
                                                <!-- mic hack pt iconita daca am avea -->
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
              
                                    <!-- Butoane -->
                                    <Button Grid.Row="2" Content="ACCEPTĂ REZERVAREA" 
                                            Command="{Binding DataContext.ConfirmCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                            CommandParameter="{Binding}"
                                            Background="#27AE60" Foreground="White" FontWeight="Bold" 
                                            Height="40" BorderThickness="0" Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="0,0,8,8" Padding="10">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#2ECC71"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
    </Grid>
</UserControl>