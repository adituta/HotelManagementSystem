<UserControl x:Class="HotelManagementSystem.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">

    <Grid>
        <!-- Aici pui codul de design -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Sistem Management Hotel" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <TextBlock Text="Utilizator:"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="25"/>

            <TextBlock Text="Parolă:" Margin="0,10,0,0"/>
            <!-- PasswordBox nu suportă binding direct din motive de securitate, 
                 îl vom gestiona în ViewModel sau folosind un Helper ulterior -->
            <PasswordBox x:Name="PasswordInput" Margin="0,5" Height="25"/>

            <Button Content="Autentificare" 
                    Command="{Binding LoginCommand}" 
                    CommandParameter="{Binding ElementName=PasswordInput}"
                    Margin="0,20,0,0" Height="35" Cursor="Hand"/>

            <Button Content="Înregistrare Client" 
                    Command="{Binding RegisterCommand}" 
                    Background="Transparent" BorderThickness="0" 
                    Foreground="Blue" Cursor="Hand" Margin="0,10"/>
        </StackPanel>
    </Grid>
</UserControl>